import { useNavigate } from "react-router-dom";
import { useEffect } from "react";

function verificarId(campoLocalStorage) {
    /*const navigate = useNavigate(); // useNavigate retorna diretamente a função de navegação
     
        useEffect(() => {
            if (campoLocalStorage && campoLocalStorage !== "" && localStorage.getItem(campoLocalStorage)) {
                return localStorage.getItem(campoLocalStorage);
            } else {
                navigate(-1);
                return null; // Evita erro de retorno indefinido
            }
        }, [navigate]); */
}

export default verificarId;